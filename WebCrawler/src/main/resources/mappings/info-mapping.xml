<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="InfoDAO">
	<insert id="insertInfo">
	<![CDATA[
		insert into INFO(seq,title,link,content,field,cssquery,id,itype)
		values((SELECT ifnull(max(seq), 0) + 1 FROM INFO a),#{title},#{link},#{content},#{field},#{cssQuery},#{id},#{itype})
	]]>
	</insert>
	<select id="getInfo" resultType="info">
		<![CDATA[
			SELECT *
			FROM INFO
			WHERE SEQ=#{seq}
		]]>
	</select>
	<select id="getInfoList" resultType="info">
		<![CDATA[
			SELECT *
			FROM INFO
			WHERE ID=#{id}
			ORDER BY SEQ DESC
		]]>
	</select>
	<delete id="deleteInfo">
	<![CDATA[
		DELETE FROM INFO
		WHERE SEQ=#{seq}
	]]>
	</delete>
</mapper>
