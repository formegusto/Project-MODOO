<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="InfoDAO">
	<insert id="insertInfo">
		insert into info(seq,title,link,content,field,cssquery,id)
		values((SELECT ifnull(max(seq), 0) + 1 FROM info a),#{title},#{link},#{content},#{field},#{cssQuery},#{id})
	</insert>
	<select id="getInfo" resultType="info">
		<![CDATA[
			SELECT *
			FROM INFO
			WHERE SEQ=#{seq}
		]]>
	</select>
	<select id="getInfoList" resultType="info">
		<![CDATA[
			SELECT *
			FROM INFO
			WHERE ID=#{id}
			ORDER BY SEQ DESC
		]]>
	</select>
</mapper>
